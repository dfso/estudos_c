#include <stdio.h>
#include <stdlib.h>

int main()
{
    char letra;
    char *code = "enigma";

    printf("??????????????????????????????????????\n");
    printf("digite a letra: ");
    scanf("%c", &letra);

    printf("chave: %d\n", letra);   // codigo ASCII

    printf("letra criptografada: %c\n", letra-16);
    printf("%s\n", code);

    for(int i=0; i<6; i++) {
        printf("%d - ", code[i]);
    }
    printf("\n");
    for(int i=0; i<6; i++) {
        printf("%c", code[i]-16);
    }

    printf("\n??????????????????????????????????????\n");
    
    return 0;
}
